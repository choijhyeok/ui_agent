# Workspace

This directory is mounted into `apps/runtime` by Docker Compose. Downstream generation issues will populate real React source files here. Foundation seeds only a minimal preview placeholder.
